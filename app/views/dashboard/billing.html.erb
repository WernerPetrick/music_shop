<%= render 'layouts/nav' %> 
<div class="container c-body">
<%= render 'layouts/tabs' %>

     <% if @user%>
            <table class="table table-striped">
                <h4 class="mt-4">Billing Details</h4>
                <tbody>
                    <tr>
                        <td>Bank Name</td>
                        <td> ADDING FUNTIONALITY LATER </td>
                    </tr>
                    <tr>
                        <td>Account Number</td>
                        <td> ADDING FUNTIONALITY LATER </td>
                    </tr>
                    <tr>
                        <td>Branch Name</td>
                        <td><%=@user.email%></td>
                    </tr>
                    <tr>
                        <td>Branch</td>
                        <td> ADDING FUNTIONALITY LATER </td>
                    </tr>
                </tbody>
            </table>
       <% else %>
            <h1>Something went horribly wrong somewhere</h1>
       <% end %>
    <%= button_to "Change Billing Details", changepass_path, class:"btn btn-info"%>
</div>